# For more information about tox, see https://tox.readthedocs.io/en/latest/
[tox]
envlist = {py36}-{lint,all}-{default}-{dev,pkg}

[_lint]
description = Flake check
deps = .[tests]
# TODO: one day...
commands =
#commands = flake8

[_all]
description = All the tests...need to clean up/separate
deps = .[tests]
       sphinx_ioam_theme
# TODO: create sample proj, fill in rest of commands
commands = nbsite_from_tmplate.py .

[testenv]
changedir = {envtmpdir}

deps = lint: {[_lint]deps}
       all: {[_all]deps}

commands = lint: {[_lint]commands}
           all: {[_all]commands}

[flake8]
include = *.py
# run_tests.py is generated by conda build, which appears to have a
# bug resulting in code being duplicated a couple of times.
exclude = .git,__pycache__,.tox,.eggs,*.egg,doc,dist,build,_build,.ipynb_checkpoints,run_test.py
ignore = E,
         W
